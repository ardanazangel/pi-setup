[orquestadores-agentes-ia] Comparativa caso Angelito (Pi, macOS): Superset (Elastic-2.0, terminal, macOS) modelo "1 tarea=1 branch=1 worktree" ceremonia git obligatoria (le da rabia). Paseo (AGPL-3, getpaseo/paseo, daemon+WebSocket+MCP, cliente Expo/RN multiplataforma, sin login) CLAVE "works in any directory, worktrees optional" → resuelve fricción git; remoto=relay cifrado/VPN no SSH; sin dashboard tokens; soporta Pi; subreddit r/PaseoAI. Conductor.build closed macOS, solo CC+Codex, NO Pi. Conductor OSS (charannyk06/conductor-oss, MIT, Rust/PTY) SÍ Pi. OpenChamber NO multi-agente, solo OpenCode, NO Pi. oh-my-pi/omp (can1357) FORK de Pi (upstream badlogic/pi-mono). Emdash (emdash.sh, YC) orquestador OSS de CLIs en paralelo en worktrees, Electron (NO fork VSCode→sin extensiones, editor Monaco minimal, "Open in" VSCode/Cursor/Zed); Angelito lo ve mejor dev-tool de los mirados pero sin LSP tipo Zed y no quiere 2 apps; su uso real (HTML más organizado) NO aprovecha el paralelismo. <!-- created=2026-06-18 --> <!-- created=2026-06-18, last=2026-06-18 -->
§
[pi-acp-zed] DOS adaptadores ACP de Pi para Zed: (A) svkozak/pi-acp = npm `pi-acp`, 421★, el más popular y el que usa Angelito (spawna `pi --mode rpc` como subproceso); (B) @victor-software-house/pi-acp = embebe pi vía SDK. Ambos: Pi NO habla ACP nativo — badlogic rechazó ACP en core, viven sobre RPC mode. EXTENSIONES en svkozak/pi-acp: comandos de `pi.registerCommand` EXPLÍCITAMENTE no soportados ("Slash commands provided by pi extensions are not currently supported"); desactivado a propósito en código (`includeExtensionCommands: false`). El startup-info block SÍ los LISTA (sección Extensions) pero es solo informativo, no ejecutable. PR #20 (RogierKonings) para habilitarlos está ABIERTO sin mergear; svkozak lo quiere opt-in vía env var `PI_ACP_ENABLE_EXTENSION_COMMANDS` porque pi-acp no emula el runtime TUI → comandos con UI custom/overlays/notificaciones se anuncian pero no funcionan bien. <!-- created=2026-06-18, last=2026-06-18 -->
§
[orquestador-emdash] Emdash (emdash.sh, YC-backed, "Over 840K downloads") = orquestador open-source de agentes CLI en paralelo, cada uno aislado en su propio git worktree. App de escritorio Electron, NO es fork de VS Code (a diferencia de Cursor/Windsurf) → NO soporta extensiones de VS Code ni Marketplace; editor/diff interno minimalista con Monaco. Tiene targets "Open in" (VS Code/Cursor/Zed) para abrir el worktree fuera. Angelito lo considera el orquestador con MEJOR experiencia de dev-tool de los que ha mirado, pero echa de menos autocompletado/LSP tipo Zed y no quiere tener dos apps abiertas a la vez. Su uso real declarado: escribir HTML más organizado → para eso el paralelismo en worktrees de Emdash NO aporta (maquinaria para un problema que no tiene). <!-- created=2026-06-18 --> <!-- created=2026-06-18, last=2026-06-18 -->
§
[ship-extension→prompt-template] /ship migrado de extensión (pi.registerCommand en extensions/ship.ts) a PROMPT TEMPLATE en ~/.pi/agent/prompts/ship.md (extensions/ship.ts borrado). Motivo: pi-acp (svkozak) NO carga comandos de extensión, pero SÍ carga prompt templates → /ship ahora funciona en TUI de Pi Y en Zed/pi-acp. Decisión prompt-vs-skill: para órdenes imperativas "hazlo ahora" (add/commit/push) usar PROMPT TEMPLATE (aparece como /ship limpio, se inyecta el texto), NO skill (skill = conocimiento que el agente consulta, aparece feo como /skill:ship). El template incluye línea para respetar instrucciones extra tras el comando (ej. "/ship no toques el changelog"). En Zed recargar sesión del agente para descubrir nuevos prompts. Resto de comandos de extensión de Angelito (/inbox /context /workflow /ssh-add /ssh-list) NO migrables tan fácil: usan UI/lógica custom, no solo prompt. <!-- created=2026-06-18, last=2026-06-18 -->
§
[gitignore-solo-agent] Repo ~/.pi: para versionar SOLO agent/ se usa patrón blocklist-by-default en .gitignore (`/*` ignora todo el root, luego excepciones `!/.gitignore` y `!/agent/`). OJO: .gitignore NO saca del índice lo ya trackeado (README.md, evals/, experiments/, etc.) → hubo que `git rm --cached` (mantiene archivos en disco, deja de versionar). Además `agent/.DS_Store` SÍ se coló al commit porque el ignore de .DS_Store no cubría rutas dentro de agent/ (revisar `**/.DS_Store`). README.md de ~/.pi eliminado a petición de Angelito. <!-- created=2026-06-18, last=2026-06-18 -->
§
[system-md-build-rule] Angelito QUITÓ de agent/SYSTEM.md (sección "Verification before completing") la regla sobre production builds. Primero pidió relajarla (build no es paso de verificación), luego "no, simplemente no lo pongas" → regla eliminada por completo, no reformulada. Lección: cuando Angelito dice "no lo pongas", quiere borrado total, no una versión suavizada. SYSTEM.md ahora termina la sección de verificación en la regla STOP "should work/looks right". <!-- created=2026-06-18, last=2026-06-18 -->