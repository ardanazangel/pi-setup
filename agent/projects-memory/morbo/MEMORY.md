Fuentes (morbo, Next.js app router en frontend/): cargar SIEMPRE las fuentes en el layout (app/layout.js), no solo con @font-face en globals.css. La fuente oficial del proyecto es PP Object Sans (Regular/Bold/Heavy woff2), NO Helvetica — globals.css declara font-family: Helvetica pero hay que usar Object Sans. woff2 en frontend/public/fonts/. <!-- created=2026-06-17, last=2026-06-17 -->
§
Carousels: el proyecto usa `smooothy` (vallafederico/smooothy, framework-agnostic, rAF nativo, sin GSAP) para sliders/carousels. Instalada en frontend/. El shop (frontend/app/shop) usa ShopCarousel.jsx (client component) con smooothy; page.js es server component que exporta metadata. <!-- created=2026-06-17, last=2026-06-17 -->
§
Smooothy parallax (morbo shop, ShopCarousel.jsx): en modo infinite, `core.parallaxValues[i]` = distancia CON SIGNO del slide al centro en unidades de slide (0 centrado, ±1 adyacentes, ±2 siguientes…), y es CONTINUA porque usa el `current` interpolado → escalados/efectos salen suaves sin transition CSS. Smooothy YA escribe `transform: translateX()` en cada `.product`, así que cualquier scale propio debe ir en un hijo wrapper (.product-inner) para no pisarlo. Patrón scale escalonado: `scale = max(0, 1 - 0.2*|dist|)` (centro 1, ±1 0.8, ±2 0.6) en el callback `onUpdate`. Update con rAF nativo, `slider.destroy()` al desmontar. <!-- created=2026-06-17, last=2026-06-17 -->
§
Fuente PP Object Sans (morbo): NO es fuente de iconos (468 glifos, solo texto/acentos/puntuación). Trae 8 flechas Unicode U+2190–2199 pero meter el carácter directo (p.ej. `↗`) lo renderiza como emoji/fuente de sistema = feo. Las flechas REALES dibujadas por el diseñador están en el stylistic set `ss02`, que convierte DÍGITOS en flechas estilo numpad: 9→↗ 8→↑ 7→↖ 6→→ 3→↘ 2→↓ 1→↙ 4→←. Uso: escribir el dígito con `style={{ fontFeatureSettings: '"ss02"' }}`. OT features disponibles: aalt, locl, salt, sinf, ss01, ss02, subs, sups. Inspeccionar woff2 con fontTools. <!-- created=2026-06-17, last=2026-06-17 -->
§
(3) El ScrollCarousel usa sticky (.tracker position:sticky top:50vh, .scroll-carousel height:300vh), NO pin de gsap; gsap solo anima x del tracker con scrub.
(4) GSAP escribe transform:translateX() en .tracker al animar x → pisa cualquier transform CSS (p.ej. translateY(-50%) para centrar con top:50vh). Fix: gestionar el Y también con GSAP via gsap.set(track,{yPercent:-50}) (compone X+Y en la misma matriz) y quitar el translateY del CSS. Regla: nunca poner CSS transform en un elemento que GSAP transforma; delegar todo a GSAP.
(5) clip-path reveal (#about-imgs .imgs-wrapper): inset(0 50% 0 50%)=cerrado centro, inset(0 0% 0 0%)=abierto. 3 ScrollTriggers scrub independientes: abrir al acabar carrusel horizontal, cerrar(x2) al entrar .services. GOTCHA: fromTo de cierre con immediateRender (default true) fija el estado inicial al montar y pisa el clip → sale abierto al cargar. Fix doble: clase CSS inicial cerrada .imgs-wrapper.clip-closed (en JSX, arranca cerrado antes del JS) + immediateRender:false en la anim de cierre. <!-- created=2026-06-18, last=2026-06-18 -->
§
Sticky text que cambia según categoría scroleada (morbo about, ServicesList.jsx): page.js es server component (exporta metadata) → no puede usar hooks. Patrón: extraer la lista a client component ServicesList.jsx que envuelve los .service como children y usa IntersectionObserver con root=#about (el scroller de Lenis, NO window) para detectar qué .service está visible y actualizar el texto del <h3 className="sticky">. Sin dependencias. <!-- created=2026-06-18 --> <!-- created=2026-06-18, last=2026-06-18 -->
§
GSAP scrub + Lenis scrollerProxy (morbo about): un ScrollTrigger con `scrub` creado antes de que las medidas estén refrescadas con el proxy de Lenis sale con start≈end (rango ~0) → la anim salta "de golpe" en vez de interpolar. Mitigar con `invalidateOnRefresh:true` + `ScrollTrigger.refresh()` tras crear el trigger. Para fades sticky simples Angelito prefirió quitar el scrub: tween normal disparado por trigger con `toggleActions` que baje opacidad y luego VUELVA a opacity 1 al revertir (no scrub). <!-- created=2026-06-18, last=2026-06-18 -->
§
GSAP scrub + easing (morbo ScrollCarousel.jsx clip-path): con `scrub:true` el `ease` SÍ aplica — se distribuye a lo largo del rango de scroll (no ignora el ease). Para que la apertura del clip-path tenga aceleración usar `ease:"power2.out"` en vez de `"none"`; cierre/carrusel quedan en `"none"`. ServicesList.jsx (sticky text por categoría): IntersectionObserver con root=#about (scroller Lenis) y `rootMargin:"-50% 0px -50% 0px"` → el activo cambia cuando cada .service cruza el centro vertical (donde queda el sticky top:48vh). Títulos con `<br/>` en el sticky h3: usar `dangerouslySetInnerHTML` (contenido estático propio, seguro). <!-- created=2026-06-18, last=2026-06-18 -->