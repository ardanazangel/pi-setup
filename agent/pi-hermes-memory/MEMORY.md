DO [react-gotchas]: (reparenting) 3rd-party libs que reparentan DOM nodes rompen el unmount de React. (a) react-three-fiber "Cannot commit the same tree" (React #177) en resize = unmount de un <Canvas> + mount de otro mismo tick; fix: gate variant a null, remount en next rAF (distinct key solo NO basta). (b) GSAP ScrollTrigger pin:true envuelve el node en .pin-spacer (lo reparenta); si React luego desmonta ese node (conditional render/breakpoint swap) → "removeChild: node not a child"; fix: wrap en wrapper estable (display:contents) para que React solo quite el wrapper. (dev-vs-prod) antes de testear un fix contra un server corriendo, confirma `next dev` (HMR, recompila) vs `next-server`/`next start` (sirve .next prebuild, no recompila) con `ps -p PID -o command=`; errores React minificados = production build → rebuild antes de re-testear. <!-- created=2026-06-16, last=2026-06-18 -->
§
DO [js-animation-patterns]: (a) Moving ALL animation CSS→JS: audit EVERY `transition` rule, not just the main transform (color, padding/indent, child els like `.arrow`/`:is-active` have their own). Pattern: interpolate ONE continuous index `ci` in rAF (easeOutCubic), per-frame derive each item's props from distance to center (`a = 1 - |v - ci|`) → continuous, no CSS transition. (b) Wheel/trackpad carousel: inertia emits events >1s, so "await neutral gap" or accumulated-px-threshold locks after one jump ("needs two scrolls"). Hybrid model: fire on FIRST event of gesture, unlock only when deltaY decays below small neutral threshold (inertia faded) + inactivity-timer backstop. Do NOT fire on direction-change (overscroll rebound sends opposite delta at gesture END → backward jump/oscillation); let real reversals wait for inactivity gap. Drop removed consts from useEffect deps. (c) Tuning subjective "feel" params (threshold/cooldown/durations): user may bounce values then say "dejalo como al principio" — record original constant values BEFORE first tweak (git diff baseline) for exact revert. <!-- created=2026-06-17, last=2026-06-17 -->
§
Verificar box-sizing antes de sumar padding. (d) `overflow-x: hidden` fuerza `overflow-y:auto` → convierte el elemento en scroll container → `position: sticky` deja de pegarse a la ventana (se pega a ese contenedor, parece que rompe el sticky). Fix: usar `overflow-x: clip` (recorta igual pero NO crea scroll container → sticky intacto; soporte Chrome/Edge 90+, FF 81+, Safari 16+). Alternativa pre-Safari16: mover el overflow-x:hidden a un wrapper exterior que NO sea ancestro del sticky. <!-- created=2026-06-17, last=2026-06-18 -->
§
(build-tooling) Antes de construir tooling, revisa si el sistema de memoria ya lo cubre (lessons negative/category/rule = post-mortems); subir un escalón suele ser regla en SYSTEM.md, no código. (acp-zed) pi-acp (Pi vía ACP en Zed) NO expone slash commands custom de extensiones (/ship,/inbox,/workflow); solo set fijo /skill:* /compact /autocompact /export /session /name /steering /follow-up /changelog (error "not a recognized command in pi-acp"). ACP no nativo (badlogic lo rechazó #175); adaptador externo @victor-software-house/pi-acp sobre RPC. Para comandos de extensión usar TUI directo. <!-- created=2026-06-18, last=2026-06-18 -->
§
DO [gsap-gotchas]: `gsap.context(fn, scopeEl)` resuelve TODO selector string de dentro como scopeEl.querySelector. Así `scrollTrigger:{scroller:"#about"}` crashea si #about es ANCESTRO del scope (querySelector→null→scroller undefined→"Cannot read properties of undefined (reading '_gsap')" en ScrollTrigger.init). Fix: pasar el ELEMENTO real (getElementById), no string, cuando está fuera del scope. Cazar stack real de error React minificado en runtime: Chrome headless --remote-debugging-port + capturar excepción vía CDP. <!-- created=2026-06-18, last=2026-06-18 -->